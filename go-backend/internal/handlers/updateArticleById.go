package handlers

import (
	"github.com/gin-gonic/gin"
)

func UpdateArticleById(c *gin.Context) {

	//// connect to database
	//database := db.DB
	////get the article's id which need to be change
	//id := c.Param("id")
	//var article model.Article
	//// create a struct
	//var updatedArticle struct {
	//}
}
